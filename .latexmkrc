# This document loads fontspec and therefore requires XeLaTeX (or LuaLaTeX).
# LaTeX Workshop's latexmk recipe reads this project-level configuration.
$out_dir = 'build';
$pdf_mode = 5;
$xelatex = 'xelatex %O %S';
